import Channel from './Channel';
/**
 * Private channel that only you and publicKey can read/write.
 * @param publicKey
 * @returns {Channel}
 */
export default function (publicKey?: any, chatLink?: string): Channel;
